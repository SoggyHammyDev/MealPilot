services:
  mealpilot:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      DATA_DIR: "/data"
      MCP_AUTH_MODE: "token"
    volumes:
      - ./data:/data
    restart: unless-stopped
