{
  "name": "mealpilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI meal planning for Umbrel with OpenAI and MCP",
  "scripts": {
    "start": "node src/server.js",
    "dev": "NODE_ENV=development node --watch src/server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/server.js && node --check src/db.js && node --check src/openai.js && node --check src/meal-plan.js && node --check src/mcp.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@modelcontextprotocol/node": "2.0.0",
    "@modelcontextprotocol/server": "2.0.0",
    "better-sqlite3": "13.0.3",
    "express": "5.2.1",
    "zod": "4.5.4"
  }
}
